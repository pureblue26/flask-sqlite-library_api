# 环境变量模板：复制为 .env 并填入真实值
# cp .env.example .env

# 安全密钥（生产环境务必换成随机长字符串！可用 `python -c "import secrets; print(secrets.token_hex(32))"` 生成）
SECRET_KEY=

# 数据库
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
SEED_USERNAME=
SEED_PASSWORD=
