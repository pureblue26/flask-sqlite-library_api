# 前端镜像：基于 nginx，托管静态页面 + 反向代理 API
ARG NGINX_IMAGE=docker.m.daocloud.io/library/nginx:alpine
FROM ${NGINX_IMAGE}

# 前端页面
COPY index.html /usr/share/nginx/html/

# Nginx 配置（托管前端 + /api 反代到后端）
COPY nginx.conf /etc/nginx/conf.d/default.conf
